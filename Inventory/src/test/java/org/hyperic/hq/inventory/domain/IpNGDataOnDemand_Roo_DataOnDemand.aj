// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.hyperic.hq.inventory.domain;

import java.util.List;
import java.util.Random;
import org.hyperic.hq.inventory.domain.IpNG;
import org.springframework.stereotype.Component;

privileged aspect IpNGDataOnDemand_Roo_DataOnDemand {
    
    declare @type: IpNGDataOnDemand: @Component;
    
    private Random IpNGDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<IpNG> IpNGDataOnDemand.data;
    
    public IpNG IpNGDataOnDemand.getNewTransientIpNG(int index) {
        org.hyperic.hq.inventory.domain.IpNG obj = new org.hyperic.hq.inventory.domain.IpNG();
        obj.setAddress("address_" + index);
        obj.setMacAddress("macAddress_" + index);
        obj.setNetmask("netmask_" + index);
        obj.setPlatform(null);
        return obj;
    }
    
    public IpNG IpNGDataOnDemand.getSpecificIpNG(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        IpNG obj = data.get(index);
        return IpNG.findIpNG(obj.getId());
    }
    
    public IpNG IpNGDataOnDemand.getRandomIpNG() {
        init();
        IpNG obj = data.get(rnd.nextInt(data.size()));
        return IpNG.findIpNG(obj.getId());
    }
    
    public boolean IpNGDataOnDemand.modifyIpNG(IpNG obj) {
        return false;
    }
    
    public void IpNGDataOnDemand.init() {
        data = org.hyperic.hq.inventory.domain.IpNG.findIpNGEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'IpNG' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<org.hyperic.hq.inventory.domain.IpNG>();
        for (int i = 0; i < 10; i++) {
            org.hyperic.hq.inventory.domain.IpNG obj = getNewTransientIpNG(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
