// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.hyperic.hq.inventory.domain;

import java.util.List;
import java.util.Random;
import org.hyperic.hq.inventory.domain.Platform;
import org.springframework.stereotype.Component;

privileged aspect PlatformDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PlatformDataOnDemand: @Component;
    
    private Random PlatformDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Platform> PlatformDataOnDemand.data;
    
    public Platform PlatformDataOnDemand.getNewTransientPlatform(int index) {
        org.hyperic.hq.inventory.domain.Platform obj = new org.hyperic.hq.inventory.domain.Platform();
        obj.setFqdn("fqdn_" + index);
        return obj;
    }
    
    public Platform PlatformDataOnDemand.getSpecificPlatform(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Platform obj = data.get(index);
        return Platform.findPlatform(obj.getId());
    }
    
    public Platform PlatformDataOnDemand.getRandomPlatform() {
        init();
        Platform obj = data.get(rnd.nextInt(data.size()));
        return Platform.findPlatform(obj.getId());
    }
    
    public boolean PlatformDataOnDemand.modifyPlatform(Platform obj) {
        return false;
    }
    
    public void PlatformDataOnDemand.init() {
        data = org.hyperic.hq.inventory.domain.Platform.findPlatformEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Platform' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<org.hyperic.hq.inventory.domain.Platform>();
        for (int i = 0; i < 10; i++) {
            org.hyperic.hq.inventory.domain.Platform obj = getNewTransientPlatform(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
