// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.hyperic.hq.inventory.domain;

import java.util.List;
import java.util.Random;
import org.hyperic.hq.inventory.domain.PlatformNG;
import org.springframework.stereotype.Component;

privileged aspect PlatformNGDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PlatformNGDataOnDemand: @Component;
    
    private Random PlatformNGDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<PlatformNG> PlatformNGDataOnDemand.data;
    
    public PlatformNG PlatformNGDataOnDemand.getNewTransientPlatformNG(int index) {
        org.hyperic.hq.inventory.domain.PlatformNG obj = new org.hyperic.hq.inventory.domain.PlatformNG();
        obj.setAgent(null);
        obj.setConfig(null);
        obj.setFqdn("fqdn_" + index);
        obj.setName("name_" + index);
        obj.setResourceType(null);
        return obj;
    }
    
    public PlatformNG PlatformNGDataOnDemand.getSpecificPlatformNG(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        PlatformNG obj = data.get(index);
        return PlatformNG.findPlatformNG(obj.getId());
    }
    
    public PlatformNG PlatformNGDataOnDemand.getRandomPlatformNG() {
        init();
        PlatformNG obj = data.get(rnd.nextInt(data.size()));
        return PlatformNG.findPlatformNG(obj.getId());
    }
    
    public boolean PlatformNGDataOnDemand.modifyPlatformNG(PlatformNG obj) {
        return false;
    }
    
    public void PlatformNGDataOnDemand.init() {
        data = org.hyperic.hq.inventory.domain.PlatformNG.findPlatformNGEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'PlatformNG' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<org.hyperic.hq.inventory.domain.PlatformNG>();
        for (int i = 0; i < 10; i++) {
            org.hyperic.hq.inventory.domain.PlatformNG obj = getNewTransientPlatformNG(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
