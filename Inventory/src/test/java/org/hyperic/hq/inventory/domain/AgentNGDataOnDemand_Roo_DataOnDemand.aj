// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.hyperic.hq.inventory.domain;

import java.util.List;
import java.util.Random;
import org.hyperic.hq.inventory.domain.AgentNG;
import org.springframework.stereotype.Component;

privileged aspect AgentNGDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AgentNGDataOnDemand: @Component;
    
    private Random AgentNGDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<AgentNG> AgentNGDataOnDemand.data;
    
    public AgentNG AgentNGDataOnDemand.getNewTransientAgentNG(int index) {
        org.hyperic.hq.inventory.domain.AgentNG obj = new org.hyperic.hq.inventory.domain.AgentNG();
        obj.setAddress("address_" + index);
        obj.setAgentToken("agentToken_" + index);
        obj.setAgentVersion("agentVersion_" + index);
        obj.setAuthToken("authToken_" + index);
        obj.setPort(new Integer(index));
        obj.setUnidirectional(Boolean.TRUE);
        return obj;
    }
    
    public AgentNG AgentNGDataOnDemand.getSpecificAgentNG(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        AgentNG obj = data.get(index);
        return AgentNG.findAgentNG(obj.getId());
    }
    
    public AgentNG AgentNGDataOnDemand.getRandomAgentNG() {
        init();
        AgentNG obj = data.get(rnd.nextInt(data.size()));
        return AgentNG.findAgentNG(obj.getId());
    }
    
    public boolean AgentNGDataOnDemand.modifyAgentNG(AgentNG obj) {
        return false;
    }
    
    public void AgentNGDataOnDemand.init() {
        data = org.hyperic.hq.inventory.domain.AgentNG.findAgentNGEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'AgentNG' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<org.hyperic.hq.inventory.domain.AgentNG>();
        for (int i = 0; i < 10; i++) {
            org.hyperic.hq.inventory.domain.AgentNG obj = getNewTransientAgentNG(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
