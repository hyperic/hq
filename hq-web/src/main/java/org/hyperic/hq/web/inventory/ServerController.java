package org.hyperic.hq.web.inventory;

import javax.servlet.http.HttpSession;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;

//@Controller
//@RequestMapping("/inventory")
public class ServerController {
	/*
	@Autowired
	public ServerController() {
		
	}

	@RequestMapping(method = RequestMethod.GET, value = "/server")
	public void getNewServer(HttpSession session) {

	}

	@RequestMapping(method = RequestMethod.POST, value = "/server")
	public void createServer(BindingResult result, HttpSession session) {

	}

	@RequestMapping(method = RequestMethod.PUT, value = "/server/{serverId}")
	public void updateServer(@PathVariable Integer serverId, HttpSession session) {
		
	}

	@RequestMapping(method = RequestMethod.DELETE, value = "/server/{serverId}")
	public void deleteServer(@PathVariable Integer serverId, HttpSession session) {
		
	}

	@RequestMapping(method = RequestMethod.GET, value = "/server/{serverId}")
	public void getServerById(@PathVariable Integer serverId, HttpSession session) {
		
	}
	
	@RequestMapping(method = RequestMethod.GET, value = "/servers")
	public void getServers(@RequestParam Integer page, @RequestParam Integer pageSize, @RequestParam String sort, @RequestParam Integer direction, HttpSession session) {
		
	}
	
	@RequestMapping(method = RequestMethod.GET, value = "/servers/types")
	public void getAllPlatformTypes(HttpSession session) {
		
	}
	
	@RequestMapping(method = RequestMethod.GET, value = "/servers/{searchText}")
	public void getServerByText(@PathVariable String searchText, @RequestParam Boolean unavailable, @RequestParam Boolean owned, @RequestParam Boolean any, @RequestParam Integer page, @RequestParam Integer pageSize, @RequestParam String sort, @RequestParam Integer direction, HttpSession session) {
		
	}
	
	@RequestMapping(method = RequestMethod.GET, value = "/servers/type/{serverTypeId}/{searchText}")
	public void getServerByText(@PathVariable Integer serverTypeId, @PathVariable String searchText, @RequestParam Boolean unavailable, @RequestParam Boolean owned, @RequestParam Boolean any, @RequestParam Integer page, @RequestParam Integer pageSize, @RequestParam String sort, @RequestParam Integer direction, HttpSession session) {
		
	}	
	*/
}

