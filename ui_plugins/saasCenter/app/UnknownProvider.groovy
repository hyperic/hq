class UnknownProvider extends CloudProvider {
                         
    UnknownProvider() {
        code     = 'UNKNOWN' 
        longName = 'UNKNOWN'

        services = [
        ]

        indicators = [
        ]
    }
}